<%= simple_form_for(@datapoint) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :goal_id %>
    <%= f.input :amount %>
    <%= f.input :date %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
