<%= link_to('Create new goal', new_goal_path) %>
<h3>Goals</h3>
<ul>
  <%- @goals.each do |goal| %>
    <li>
      <%= link_to "%{goal}" % {:goal => goal.objective}, goal_path(goal) %>
    </li>
  <%- end %>
</ul>
